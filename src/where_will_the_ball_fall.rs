use crate::Solution;

// https://leetcode.cn/problems/where-will-the-ball-fall/

impl Solution {
    pub fn find_ball(grid: Vec<Vec<i32>>) -> Vec<i32> {
        let result = Vec::with_capacity(grid.len());

        result
    }
}
